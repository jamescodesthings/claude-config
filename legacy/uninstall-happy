#!/usr/bin/env zsh
# Remove happy

source "${0:a:h}/../tools/lib"

if ! command -v happy &> /dev/null; then
  success "happy not installed, skipping"
  exit 0
fi

npm uninstall -g happy
success "happy removed"
