# config/env.local — per-machine secrets (gitignored)
# Copy this file to config/env.local and fill in your secrets.
# ./install sources this into ~/.zshenv so Claude Code and all tools inherit the vars.

# GitHub Personal Access Token — needed by the github plugin
# GITHUB_PERSONAL_ACCESS_TOKEN=ghp_...

# Add other machine-specific env vars below:
